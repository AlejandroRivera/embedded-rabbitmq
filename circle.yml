## Customize the test machine
machine:
  cache_directories:
    - "~/.m2/repository"
    - "~/.embeddedrabbitmq"

test:
  override:
    - erl -eval 'erlang:display(erlang:system_info(otp_release)), halt().'  -noshell
    - mvn clean install
